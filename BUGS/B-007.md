# B-007: "Could not determine a page template to use" alert

**Status**: Invalid — reported against the wrong plugin, not applicable here.

## Symptom
Alert/toast reading "could not determine a page template to use" appeared
during testing. Exact trigger (which operation, if any) not yet known.

## Hypothesis
None yet. The message doesn't appear anywhere in our source (`grep` across
`src/`, `App.tsx`, `index.js` — no match), so it's not an alert our plugin
generates directly. It could be:
- A native Supernote-level message unrelated to the plugin (e.g. triggered by
  normal page navigation/creation happening around the same time).
- A native error surfacing indirectly as a side effect of one of our SDK
  calls (insertElements / deleteElements / modifyElements / reloadFile /
  saveCurrentNote), if one of them is unexpectedly causing the host to think
  a new page needs to be created.

Genuinely unclear without more context — not speculating further yet.

## Evidence
None yet.

## Instrumentation
Not yet added.

## Next step
Ask the user exactly what they were doing when it appeared (which plugin
operation, if any — Collapse / Expand / Recollapse / Name — or unrelated
page navigation) and what happened right before/after (did the operation
otherwise succeed or fail, did the page change, etc.).
